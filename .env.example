# Copy this file to .env and edit values.

# Admin login for the web UI and API
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_now

# Default RTMP URL used initially (can be changed from UI)
DEFAULT_RTMP_URL=rtmp://example.com/live/streamkey

# Host/port for Flask server
HOST=127.0.0.1
PORT=9000

# Video size for ffmpeg scaling
VIDEO_SIZE=1920x1080

# Optional explicit ffmpeg path (e.g. on Windows)
# FFMPEG_PATH=F:/ffmpeg-8.0.1-essentials_build/bin/ffmpeg.exe
# If omitted, defaults to "ffmpeg"

# Optional ffprobe path (for track durations)
# FFPROBE_PATH=F:/ffmpeg-8.0.1-essentials_build/bin/ffprobe.exe
# If omitted, defaults to "ffprobe"

# Optional custom uploads directory
# UPLOAD_DIR=F:/Focus Music/uploads

# Optional custom config path
# CONFIG_PATH=F:/Focus Music/rtmp_music_bot_config.json
